#script for pipeline labo.
bamtobwasplit.sh is used to convert bam file to fastq and then split file into mulitple files. Will create 3 script to launch the pipleine on the selected file. 
launchbwa.sh is used by one of the script above.
coverage.sh is used to calculate depth/coverage
